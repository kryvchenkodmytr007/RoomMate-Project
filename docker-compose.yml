services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: roommate
      POSTGRES_USER: roommate
      POSTGRES_PASSWORD: secret
    ports:
      - '5432:5432'
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
